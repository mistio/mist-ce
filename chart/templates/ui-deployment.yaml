apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -c
    kompose.version: 1.8.0 (0c0c027)
  creationTimestamp: null
  labels:
    io.kompose.service: ui
  name: ui
spec:
  replicas: 1
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        io.kompose.service: ui
    spec:
      containers:
      - args:
        - sh
        - /entry.sh
        image: registry.gitlab.com/clearglass/clearglass-community/ui:{{ .Values.IMAGE_TAG }}
        name: ui
        resources: {}
        stdin: true
        tty: true
      restartPolicy: Always
status: {}
