[[timescale]]
name = "timescaledb"
bind-addr = "0.0.0.0:9096"
output = [
    { name="timescale_test", connstr = "host=timescaledb dbname=tutorial user=root password=example sslmode=disable", table= "metrics" },
]
