kind: Service
apiVersion: v1
metadata:
  name: landing
  namespace: REPLACE_NAMESPACE
  labels:
    name: landing
    component: REPLACE_STACK
spec:
  ports:
    - port: 80
  selector:
    app: landing
    component: REPLACE_STACK
