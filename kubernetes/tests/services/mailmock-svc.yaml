kind: Service
apiVersion: v1
metadata:
  name: mailmock
  namespace: REPLACE_NAMESPACE
  labels:
    name: mailmock
    component: REPLACE_STACK
spec:
  type: NodePort
  ports:
    - port: 8025
      nodePort: 30007
      name: imap
  selector:
    app: mailmock
    component: REPLACE_STACK
