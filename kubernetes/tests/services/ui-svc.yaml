kind: Service
apiVersion: v1
metadata:
  name: ui
  namespace: REPLACE_NAMESPACE
  labels:
    name: ui
    component: REPLACE_STACK
spec:
  ports:
    - port: 80
  selector:
    app: ui
    component: REPLACE_STACK
